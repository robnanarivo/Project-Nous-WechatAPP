<!--pages/course/course.wxml-->

<mp-cells title="课程列表">
  <mp-checkbox-group prop="radio" multi="{{false}}">
    <mp-checkbox wx:for="{{courseInfo}}" label="{{item.courseName}}" value="{{item.courseID}}"></mp-checkbox>
  </mp-checkbox-group>
</mp-cells>