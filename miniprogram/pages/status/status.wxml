<!--pages/status/status.wxml-->

<text wx:if="{{accepted && reviewed}}">已录取</text>
<text wx:if="{{accepted === false && reviewed}}">已拒绝</text>
<text wx:if="{{reviewed === false}}">尚未审阅</text>

  <!-- Course List -->
  <view wx:if="{{accepted && reviewed}}" class="uploader">
    <navigator url="../course/course" open-type="navigate" class="uploader-text">
      <text>我要选课</text>
    </navigator>
  </view>